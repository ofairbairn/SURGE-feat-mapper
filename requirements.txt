# Core dependencies
numpy>=1.20.0
scipy>=1.5.0
scikit-learn>=1.0.0
joblib>=1.0.0
pandas>=1.5.0
pyarrow>=10.0.0
pyyaml>=6.0.0

# Visualization (required for plotting functions)
matplotlib>=3.5.0
bokeh>=3.4.0
holoviews>=1.19.0
datashader>=0.16.0
hdbscan>=0.8.40

# Optional dependencies for enhanced models
# PyTorch for enhanced MLP models
torch>=1.9.0

# Hyperparameter optimization
optuna>=3.0.0
scikit-optimize>=0.9.0

# GPflow for advanced Gaussian Process models
# On Apple Silicon (Darwin arm64) install mainline tensorflow (NOT tensorflow-macos)
# because tensorflow-macos was abandoned at 2.16.2 and is incompatible with NumPy 2.x.
# Use --no-deps for gpflow to bypass its stale tensorflow-macos platform marker:
#
#   pip install "tensorflow>=2.21" "tf-keras>=2.21" "tensorflow-probability==0.25.0"
#   pip install "gpflow==2.10.1" --no-deps
#
# Tested: tensorflow==2.21.0 + tensorflow-probability==0.25.0 + gpflow==2.10.1 + numpy>=2.0
tensorflow>=2.21.0; platform_machine != "arm64" or platform_system != "Darwin"
# On Apple Silicon, install tensorflow-macos is NOT recommended; see note above.
tf-keras>=2.21
tensorflow-probability>=0.25.0
# gpflow must be installed separately with --no-deps on Apple Silicon arm64

# Development and testing (optional)
# pytest>=6.0
# pytest-cov
# ruff>=0.1.0
# black
# isort
# mypy
