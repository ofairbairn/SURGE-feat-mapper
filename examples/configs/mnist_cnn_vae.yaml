# MNIST: CNN + VAE classification workflow

dataset_path: data/datasets/mnist_converted/mnist_train.parquet
dataset_format: auto

metadata_overrides:
  outputs: ["digit"]

task_type: classification

test_fraction: 0.2
val_fraction: 0.1

standardize_inputs: false
standardize_outputs: false

seed: 42

output_dir: runs/mnist
run_tag: mnist_cnn_vae
overwrite_existing_run: true
predictions_format: parquet

models:
  - key: pytorch.mnist_cnn
    name: MNIST_CNN
    params:
      epochs: 12
      batch_size: 32
      learning_rate: 0.001
      device: cpu
    store_predictions: true

  - key: pytorch.vae
    name: MNIST_VAE
    params:
      task: classification
      latent_dim: 16
      hidden_dim: 128
      beta: 1.0
      n_epochs: 80
      batch_size: 256
      learning_rate: 0.001
      patience: 10
      random_state: 42
    store_predictions: true
